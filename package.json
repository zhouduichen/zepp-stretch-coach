{
  "name": "stretch-coach",
  "version": "1.0.1",
  "description": "Stretch coach for post-workout recovery on Zepp OS",
  "type": "module",
  "main": "app.js",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "validate": "node scripts/validate-content.mjs",
    "validate:assets": "node scripts/validate-assets.mjs",
    "verify": "npm run test && npm run validate && npm run validate:assets"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@zeppos/device-types": "^3.0.0"
  },
  "dependencies": {
    "@zeppos/zml": "^0.0.9"
  }
}
